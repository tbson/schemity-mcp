# Stdio. Claude Code starts Schemity itself when it is closed.
# The MCP panel in Schemity shows this command with the path for your platform.
claude mcp add --scope user schemity -- "/Applications/Schemity.app/Contents/MacOS/schemity" --mcp
